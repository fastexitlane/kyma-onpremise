apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: config
data:
  config: |
    address-pools:
    - name: default
      protocol: layer2
      addresses:
      {% for ipadress in metallb_loadbalancing_ips %}
        - {{ ipadress }}/32
      {% endfor %}